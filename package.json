{
  "name": "epitech-coding-style-checker",
  "module": "src/main.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run src/main.ts",
    "bundle": "bun build --entrypoint src/main.ts --target bun --minify --outdir dist/"
  },
  "devDependencies": {
    "@types/bun": "^1.1.13",
    "@types/node": "^22.9.0"
  },
  "peerDependencies": {
    "typescript": "^5.6.3"
  }
}
